import { CriteriaFilterPipe } from './criteria-filter.pipe';

describe('CriteriaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CriteriaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
